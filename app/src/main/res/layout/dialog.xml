<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    >

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="enter the max number of overs"
        android:textColor="@color/textcolor"
        android:id="@+id/textView"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="8dp" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:digits="0123456789."
        android:inputType="number"
        android:ems="10"
        android:backgroundTint="#24acf5"
        android:textColor="@color/textcolor"
        android:id="@+id/minovers"
        android:layout_below="@+id/textView"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignRight="@+id/textView"
        android:layout_alignEnd="@+id/textView" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:text="DONE"
        android:id="@+id/done"
android:onClick="done"
        android:textColor="#24acf5"
        android:layout_alignTop="@+id/exit"
        android:layout_alignRight="@+id/minovers"
        android:layout_alignEnd="@+id/minovers" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="EXIT APP"
        android:textColor="#24acf5"
        android:background="@android:color/transparent"

        android:onClick="exit"
        android:id="@+id/exit"
        android:layout_below="@+id/minovers"
        android:layout_toLeftOf="@+id/done"
        android:layout_toStartOf="@+id/done"
        android:layout_marginTop="7dp" />

</RelativeLayout>